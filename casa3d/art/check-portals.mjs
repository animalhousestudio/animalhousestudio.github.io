import {readFileSync} from 'node:fs';import {GLTFLoader} from 'three/addons/loaders/GLTFLoader.js';import {Raycaster,Vector3} from 'three';
const b=readFileSync(new URL('../src/assets/models/mansion-v04.glb',import.meta.url));const {scene}=await new GLTFLoader().parseAsync(b.buffer.slice(b.byteOffset,b.byteOffset+b.byteLength),'');scene.updateMatrixWorld(true);
for(const [name,start,end] of [['veranda',[-6,1.82,1.2],[-10,1.82,1.2]],['voliera',[-6,10.25,.3],[-10,10.25,.3]],['east',[6.7,10.25,.1],[14,10.25,.1]],['west',[-6,17.6,3],[-12.5,17.6,4.5]]]){const a=new Vector3(...start),z=new Vector3(...end),r=new Raycaster(a,z.clone().sub(a).normalize(),0,a.distanceTo(z));console.log(name,r.intersectObject(scene,true).map(h=>[h.object.name,h.distance.toFixed(2)]));}
